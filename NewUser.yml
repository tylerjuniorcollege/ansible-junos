---
 - name: Rollback configuration on junos devices
   hosts: test
   roles:       
    - Juniper.junos
   connection: local
   gather_facts: no
   
   - name: create new user account
  junos_user:
    name: {{ Enter Name }}
    state: present
