---
- name: deleting interface range
  hosts: Junos_all
  connection: local
  gather_facts: no
  
  tasks:
  
    - name: collecting info
      junos_config:
        lines:
          - "run show interface interface-range XIRRUS-WAP"
     #   junos_facts:
     #   gather_network_resources: interfaces
