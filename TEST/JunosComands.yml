---
- name: adding command
  hosts: Junos_all
  gather_facts: no

 # vars:

  tasks:
    - name: show command
      junos_command:
        commands: show ethernet-switching table
        wait_for: results[0] contains 00:10:8d
