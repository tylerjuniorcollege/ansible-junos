---
- name: adding command
  hosts: Junos_all
  gather_facts: yes

  vars:
    ansible_connection: netconf
    ansible_command_timeout: 60


  tasks:
    - name: show version
      junos_command:
        commands: "show version"
