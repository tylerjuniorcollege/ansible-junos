---
 - name: Change Vlan Name
   hosts: Junos_all
   roles:
    - Juniper.junos
   connection: local
   gather_facts: no
   
   tasks:
- name: configure VLAN ID and name
  junos_vlan:
    vlan_name: test
    vlan_id: 20
    name: test-vlan
