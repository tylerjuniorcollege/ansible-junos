---
- name: show ethernet
  hosts: Junos_all
  gather_facts: yes

  vars:
    ansible_connection: netconf
    ansible_command_timeout: 60


  tasks:
    - name: tacking down device
      junos_command:
        commands: show ethernet-switching table | match 78:a1:83