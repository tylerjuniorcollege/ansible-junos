---
- name: adding command
  hosts: Junos_all
  roles:       
    - Juniper.junos
  gather_facts: no

 # vars:

  tasks:
    - name: show command
      junos_command:
        commands: "show ethernet-switching table"
    #    Match wait_for: result[0] contains "00:10:8d"
    #    Match: "00:10:8d"
