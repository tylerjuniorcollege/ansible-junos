---
 - name: Rollback configuration on junos devices
   hosts: Junos_test
   roles:       
    - Juniper.junos
   connection: local
   gather_facts: no

   tasks:

    - name: remove a user account
      junos_user:
       name: shane
       state: absent
