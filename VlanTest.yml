---
 - name: VLAN
   hosts: Junos_Test
   connection: local
   gather_facts: no
   tasks:

     - name: configure VLAN ID and name
       junos_vlan:
         name: Media
         vlan_id: 20
         name: Media 
