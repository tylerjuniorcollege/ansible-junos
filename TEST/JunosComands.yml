---
- name: adding command
  hosts: Junos_all
  roles:       
    - Juniper.junos
  gather_facts: no

 # vars:

  tasks:
    - name: show command
      junos_command:
        commands: "show ethernet-switching table"
        wait_for: 
        Match: display 00:10:8d
