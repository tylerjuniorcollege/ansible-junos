---
 - name: VLAN
   hosts: Junos_Test
   connection: local
   gather_facts: no
   tasks:

    - name: remove VLAN configuration
      junos_vlan:
       vlan_id: 20
       state: absent
    
    - name: configure VLAN ID and name
      junos_vlan:
       name: Media
       vlan_id: 20 
