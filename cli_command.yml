---
- name: cli command
  hosts: Junos_all
  gather_facts: no

  vars: 
    ansible_connection: network_cli

  tasks:
    - name: show command
      cli_command:
        command: "edit interface {{ irb }}"
        new line: "replace pattern Test with test12"





# show ethernet-switching table | match {{MAC}}
